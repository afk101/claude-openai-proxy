# 02 — 流式请求保持上下文 Header 行为一致

**What to build:** IDE 用户通过流式 Chat Completions 调用代理时，获得与非流式请求完全一致的来源、任务和追踪上下文行为，同时保持现有 SSE、错误传播和资源释放语义。

**Blocked by:** 01 — 非流式请求补全并保留上游上下文 Header

**Status:** resolved

- [x] 流式请求的真实上游 HTTP Header 遵守与 Ticket 01 相同的保留与生成规则。
- [x] 流式与非流式路径复用同一上下文解析职责，不产生规则漂移。
- [x] 现有流式预连接错误、SSE 转换、取消和资源清理测试保持通过。
- [x] 完整测试套件通过，规格中的 Header 安全边界得到回归覆盖。
